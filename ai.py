import maze as create
maze = create.main()

def ComputePath():


def main():
    # Initialize start of search
    counter = 0
    # Call A* whenever run into a wall
    while true:
        if wall:
            ComputePath()
